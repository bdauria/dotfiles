'use strict';

exports.__esModule = true;
var name = exports.name = 'Mocha template';

var dispatcher = exports.dispatcher = function dispatcher(_ref) {
  var action = _ref.action;
  return action + ';';
};

var assertion = exports.assertion = function assertion(_ref2) {
  var path = _ref2.path;
  var curState = _ref2.curState;
  return 'expect(store' + path + ').toEqual(' + curState + ');';
};

var wrap = exports.wrap = function wrap(_ref3) {
  var name = _ref3.name;
  var actionName = _ref3.actionName;
  var initialState = _ref3.initialState;
  var assertions = _ref3.assertions;
  return 'import expect from \'expect\';\nimport ' + name + ' from \'../../stores/' + name + '\';\n\ndescribe(\'' + name + '\', () => {\n  it(\'' + actionName + '\', () => {\n    const store = new ' + name + '(' + initialState + ');\n    ' + assertions + '\n  });\n});\n';
};

exports.default = { name: name, assertion: assertion, dispatcher: dispatcher, wrap: wrap };