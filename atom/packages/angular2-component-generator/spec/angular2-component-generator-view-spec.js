'use babel';

import Angular2ComponentGeneratorView from '../lib/angular2-component-generator-view';

describe('Angular2ComponentGeneratorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
