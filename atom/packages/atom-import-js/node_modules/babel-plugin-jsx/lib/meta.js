function getPropsMeta(props, scope, file, t) {
  var meta = {};

  for (var i = 0, len = props.length; i < len; i++) {
    var prop = props[i];

    if (t.isJSXSpreadAttribute(prop)) {
      return null;
    } else {
      propsStack.push(makeProp(prop));
    }
  }

  return meta;
}