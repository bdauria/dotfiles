'use strict';

var PREFIX_REGEX = /[^ ,{}();'"]$/;

function shouldTriggerAutocomplete(_ref) {
  var editor = _ref.editor;
  var activatedManually = _ref.activatedManually;
  var bufferPosition = _ref.bufferPosition;

  if (activatedManually) {
    return true;
  }
  var prefix = editor.getTextInBufferRange([[bufferPosition.row, 0], bufferPosition]);
  return PREFIX_REGEX.test(prefix);
}

module.exports = { shouldTriggerAutocomplete: shouldTriggerAutocomplete };